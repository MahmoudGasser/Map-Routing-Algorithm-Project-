# Map-Routing-Algorithm-Project-
This project implements a map routing system that finds the shortest path between nodes using Dijkstra’s algorithm. The system can load map data from files and allows users to query routes efficiently.
